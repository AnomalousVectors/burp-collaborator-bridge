/**
 * Tiny HTTP/1.1 transport helpers used by local bridge servers.
 *
 * <p>Kept free of Collaborator / Montoya types so a future RPC bridge can reuse the same parser
 * and JSON response writer.</p>
 */
package ai.anomalousvectors.tools.burp.bridge.http;
