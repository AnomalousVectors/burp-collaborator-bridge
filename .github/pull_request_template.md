## Summary

-

## Test plan

- [ ] `./gradlew clean build` (or `.\gradlew.bat clean build`) passes locally
- [ ] Spotless / SpotBugs clean when applicable
- [ ] Manual Burp load checked if UI or bridge behavior changed
